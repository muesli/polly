<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#main-navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      {{#link-to 'index' class="navbar-brand"}}Polly{{/link-to}}
    </div>

    <div class="collapse navbar-collapse" id="main-navbar">
      <ul class="nav navbar-nav">
            {{#link-to 'proposals.all' tagName="li"}}<a href="">Proposals</a>{{/link-to}}
            {{#if (not-eq currentUser.user.id "1")}}
              {{#link-to 'proposals.new' tagName="li"}}<a href="">Add your Proposal</a>{{/link-to}}
            {{/if}}
      </ul>
      <ul class="nav navbar-nav navbar-right">
            {{#if (eq currentUser.user.id "1")}}
              {{#link-to 'admin' tagName="li"}}<a href="">Admin</a>{{/link-to}}
            {{/if}}
            {{#if session.isAuthenticated}}
              <li><a href="" {{action 'invalidateSession'}}>Logout
                  {{#if currentUser.user}}
                    ({{currentUser.user.email}})
                  {{/if}}
              </a></li>
            {{else}}
              {{#link-to 'login' tagName="li"}}<a href="">Login</a>{{/link-to}}
            {{/if}}
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
